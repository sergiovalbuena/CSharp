using System;

namespace methods
{
    class MainClass
    {
        public static void Main(string[] args)
            //modifier / return type/ method name / parameters. (no semi colun and Pascal cases)
            //is VOID cuando no tienen RETURN solo Console
            //METHODS: has always () at the end, even if there is not parameters 
        {
            int a = 5;
            int b = 9;
            //Console.WriteLine("The sum of 10 and 24 is {0}", AddTwoNumbers(10,24));
            Console.WriteLine("The sum of {0} and {1} is {2}", a, b, AddTwoNumbers(a, b));
        }

        //RETURN TYPE:
        static int AddTwoNumbers(int a, int b)
        {
            return a + b;
        }
    }
}

//PARAMETERS:
//placeholders to the information is used inside the method
//parameters are when decalred the method....when call the method we use PARAMETERS.

//READ
abstractnumber = Read();
WrtileLine("bla blab bla dobla dip doblada"+(char)Read());
ReadLine(); //retunrs a String

Write("Enter your age:");
aValue + ReadLine();

age = int.Parse(aValue);
Convert Class
      Convert.toDouble();
        Convert.ToInt32();

WriteLine("YOur age next year" + "will be {0}", ++age);

Readkey(); //

//MATH METHODS
Math.Floor(aValue) //result 78. ......de 78.93
Math.Sqty(aValue);

WRITING YOU OWN CALSS METHODS

    write("enter');
    inputValue = ReadLine();